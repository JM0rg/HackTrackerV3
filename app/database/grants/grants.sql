-- Reference copy of database GRANTs. This file is REGENERATED by
-- `npm run fetch-all` after a successful deploy — do not hand-edit.
--
-- The authoritative grants live in the migration files
-- (app/database/migrations/*_grants.sql). Until the schema is first deployed
-- and fetched, this placeholder keeps validate-rpc-grants.js satisfied
-- (no fetched function reference copies exist yet, so there is nothing to
-- cross-check).
